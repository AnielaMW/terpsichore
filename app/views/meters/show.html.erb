<main class="meter-show">
  <% if @admin_user %>
  <header>
    <ul class="user-bar">
      <li><%= link_to image_tag("icons/svg/trashcan", alt:"Delete", title:"Delete", class:"icon"), @meter, method: :delete, data: { confirm: 'Are you sure?' } %></li>
      <li><%= link_to image_tag("icons/svg/pencil", alt:"Edit", title:"Edit",
      class:"icon"), edit_meter_path(@meter) %></li>
    </ul>
  </header>
  <% end %>

  <h1><%= @meter.name %></h1>
</main>
