<main class="comment-type-show">
  <% if @admin_user %>
    <header>
      <ul class="user-bar">
        <li><%= link_to image_tag("icons/svg/trashcan", alt:"Delete", class:"icon"), @comment_type, method: :delete, data: { confirm: 'Are you sure?' } %></li>
        <li><%= link_to 'Edit', edit_comment_type_path(@comment_type) %></li>
      </ul>
    </header>
  <% end %>

  <h2><%= @comment_type.name %></h2>
  <p><%= @comment_type.description %></p>
</main>
