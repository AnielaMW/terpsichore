<main class="dance_level-show">
  <% if @admin_user %>
    <aside>
      <ul class="user-bar">
        <li><%= image_tag("icons/svg/trashcan", alt:"Delete", class:"icon"), @dance_level, method: :delete, data: { confirm: 'Are you sure?' } %></li>
        <li><%= link_to 'Edit', edit_dance_level_path(@dance_level) %></li>
      </ul>
    </aside>
  <% end %>

  <h2><%= @dance_level.name %></h2>
  <p><%= @dance_level.description %></p>
</main>
